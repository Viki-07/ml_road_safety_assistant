name: awake_safe
description: Demonstrates usecase of ML in road safety.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_pdfview: ^1.3.2
  image_picker: ^1.1.2
  camera: ^0.11.3
  path: ^1.9.0
  path_provider: ^2.1.4
  audioplayers: ^6.5.1
  shared_preferences: ^2.0.13

  google_mlkit_commons: any

  google_ml_kit:
    path: lib/ml_kit/google_ml_kit

  google_mlkit_face_detection:
    path: lib/ml_kit/google_mlkit_face_detection
  google_mlkit_face_mesh_detection:
    path: lib/ml_kit/google_mlkit_face_mesh_detection

  provider: ^6.1.2
  http: ^1.5.0
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  location: ^8.0.1
  quickalert: ^1.1.0
  sensors_plus: ^7.0.0
  simple_animation_progress_bar: ^1.8.2
  digital_lcd_number: ^0.1.2
  lcd_led: ^1.0.0
  flutter_speedometer_new: ^1.0.0
  lcd_number_text: ^0.0.2
  flutter_localizations:
    sdk: flutter
  syncfusion_localizations: ^31.2.5
  desktop_window: ^0.4.1
  syncfusion_flutter_sliders: ^31.2.5
  cherry_toast: ^1.11.0
  rename_app: ^1.6.1




dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

dependency_overrides:
  google_mlkit_face_detection:
    path: lib/ml_kit/google_mlkit_face_detection
  google_mlkit_face_mesh_detection:
    path: lib/ml_kit/google_mlkit_face_mesh_detection

flutter:
  uses-material-design: true
  assets:
    - assets/

  fonts:
    - family: digital
      fonts:
        - asset: assets/digital-7.ttf
        
